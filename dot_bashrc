case "$(hostname)" in
  unas)    [ -f ~/.bashrc.unas ]    && source ~/.bashrc.unas ;;
  qnas)    [ -f ~/.bashrc.qnas ]    && source ~/.bashrc.qnas ;;
  delltop) [ -f ~/.bashrc.delltop ] && source ~/.bashrc.delltop ;;
  termux)       [ -f ~/.bashrc.termux ]  && source ~/.bashrc.termux ;;
esac

